import { useState } from 'react';
import styles from './PinEntryPreview.module.css';
export function PinEntryPreview(){const [pin,setPin]=useState('');const add=(n:string)=>setPin(value=>(value+n).slice(0,4));return <main className={styles.page} data-implementation-mode="ui-only" data-canonical-screen-id="1j"><section><div className={styles.avatar}>서</div><h1>서연</h1><p>PIN 번호 4자리를 입력해 주세요</p><div className={styles.dots}>{[0,1,2,3].map(index=><i key={index} className={index<pin.length?styles.filled:''}/>)}</div><div className={styles.keys}>{['1','2','3','4','5','6','7','8','9','PIN 찾기','0','←'].map(key=><button key={key} type="button" onClick={()=>key==='←'?setPin(value=>value.slice(0,-1)):key!=='PIN 찾기'&&add(key)}>{key}</button>)}</div><button type="button" className={styles.other}>다른 프로필로 로그인</button></section></main>}
