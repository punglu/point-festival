import type React from 'react';
import styles from './FamilyAlbumPreview.module.css';
const recent = ['사진 1','사진 2','사진 3','사진 4','사진 5','+7'];
const albums = [['여름 여행','24장 · 어제','아빠'],['엄마 생일','18장 · 7월 15일','서연'],['주말 나들이','32장 · 7월 6일','민준']];
export function FamilyAlbumPreview(){const noOp=()=>undefined;return <main className={styles.page} data-implementation-mode="ui-only" data-canonical-screen-id="1h"><section className={styles.content}><header><button type="button" onClick={noOp}>←</button><div><h1>앨범</h1><span>사진 428장 · 앨범 6개</span></div><button type="button" onClick={noOp}>⌕</button><button type="button" className={styles.cta} onClick={noOp}>＋ 사진 올리기</button></header><div className={styles.filters}>{['전체','여행','기념일','일상'].map((item,index)=><button type="button" key={item} className={index===0?styles.selected:''} onClick={noOp}>{item}</button>)}</div><section className={styles.hero}><small>대표 사진 (16:9)</small><div><span>이번 주 하이라이트</span><h2>여름 여행 · 남해</h2><p>7월 22일 · 사진 24장 · 아빠가 추가</p></div><button type="button" onClick={noOp}>모두 보기</button></section><Section title="최근 추가된 사진" meta="7월 22일 · 12장"><div className={styles.photoGrid}>{recent.map((photo,index)=><button key={photo} type="button" onClick={noOp} className={index===5?styles.more:''}>{photo}{index===2&&<i>▶</i>}</button>)}</div></Section><Section title="가족 앨범" meta="전체 보기 ›"><div className={styles.albumGrid}>{albums.map(([title,meta,person])=><article key={title}><div className={styles.cover}/><div><b>{title}</b><span>{meta}</span></div><em>{person}</em></article>)}<button type="button" onClick={noOp} className={styles.create}>＋<b>앨범 만들기</b><span>주제별로 모아보기</span></button></div></Section><aside className={styles.notice}>▣<div><b>사진은 가족 구성원만 볼 수 있어요.</b><span>앨범별로 공개 범위를 따로 설정할 수 있습니다.</span></div></aside></section><nav className={styles.dock}>{['홈','포인트 잔치','대화','나'].map(label=><button key={label} type="button">○<span>{label}</span></button>)}</nav></main>}
function Section({title,meta,children}:{title:string;meta:string;children:React.ReactNode}){return <section className={styles.section}><header><b>{title}</b><span>{meta}</span></header>{children}</section>}
