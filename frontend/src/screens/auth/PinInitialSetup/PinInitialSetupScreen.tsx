import styles from './PinInitialSetupScreen.module.css';
import type { PinInitialSetupProps } from './types';
export function PinInitialSetupScreen({model,onKeyPress,onBack}:PinInitialSetupProps){const keys=['1','2','3','4','5','6','7','8','9','','0','⌫'];return <main className={styles.screen} data-canonical-screen-id="2s" data-canonical-screen-label="PIN 최초 설정" data-canonical-source="wave7-full-authority"><header><button type="button" onClick={onBack}>←</button><span>{model.step}</span></header><section><div className={styles.lock}>⌁</div><h1>{model.title}</h1><p>{model.description}</p><div className={styles.dots}>{Array.from({length:6},(_,i)=><i key={i}/>)}</div></section><div className={styles.keypad}>{keys.map(key=>key?<button key={key} type="button" onClick={()=>onKeyPress?.(key)}>{key}</button>:<span key="blank"/>)}</div></main>}
