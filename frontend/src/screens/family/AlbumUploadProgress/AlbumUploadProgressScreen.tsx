import styles from './AlbumUploadProgressScreen.module.css';
import type { AlbumUploadProgressProps } from './types';
export function AlbumUploadProgressScreen({model,onClose}:AlbumUploadProgressProps){const progress=Math.round(model.uploaded/model.total*100);return <main className={styles.screen} data-canonical-screen-id="2v" data-canonical-screen-label="앨범 업로드 진행" data-canonical-source="wave7-full-authority"><header><button type="button" onClick={onClose}>←</button><h1>{model.title}</h1><button type="button" onClick={onClose}>닫기</button></header><section className={styles.progress}><i>▧</i><strong>{model.uploaded} / {model.total}</strong><span>사진을 안전하게 올리고 있어요.</span><div><b style={{width:`${progress}%`}}/></div></section><section className={styles.list}>{model.items.map((item,index)=><article key={item.name}><i className={index<model.uploaded?styles.complete:styles.wait}>▧</i><span>{item.name}</span><em>{item.status}</em></article>)}</section></main>}
