import styles from './InvitationListScreen.module.css';
import type { InvitationListProps } from './types';
export function InvitationListScreen({model,onBack,onResend,onCancel}:InvitationListProps){return <main className={styles.screen} data-canonical-screen-id="2p" data-canonical-screen-label="초대 목록" data-canonical-source="wave7-full-authority"><header><div>{onBack&&<button type="button" className={styles.back} onClick={onBack}>←</button>}<span>가족 관리</span><h1>{model.title}</h1><p>가족에 보낸 초대를 관리할 수 있어요.</p></div><button type="button">새 가족 초대</button></header><section>{model.invitations.map(invite=><article key={invite.email}><div className={styles.avatar}>{invite.name.slice(0,1)}</div><div><strong>{invite.name}</strong><span>{invite.email}</span><small>{invite.sentAt}</small></div><em>{invite.status}</em><div className={styles.actions}><button type="button" onClick={()=>onResend?.(invite.email)}>다시 보내기</button><button type="button" onClick={()=>onCancel?.(invite.email)}>취소</button></div></article>)}</section></main>}
