# Synthetic local-only credentials for the isolated Phase 0 Compose project.
# Use with: docker compose -p mc_phase0 --env-file .env.phase0.example -f docker-compose.phase0.yml up -d --build
PHASE0_DB_NAME=mc_festival_phase0
PHASE0_DB_USER=mc_phase0
PHASE0_DB_PASSWORD=phase0-local-only-not-production
PHASE0_JWT_SECRET=phase0-local-only-not-production-change-before-exposure
