# PM Decision Brief V2

Task: MONGLE-W6-0B-CURRENT-IMPLEMENTATION-AUDIT-001 (Section 24). Every item below is a decision this
analysis is explicitly forbidden from making itself — each carries evidence, two options, a
non-binding recommendation, a risk note, and a default-prohibition (what must NOT happen by default
until the PM decides).

| decision_id | Topic | Affected screen | Evidence | Option A | Option B | Recommendation | Risk | Default prohibition | Decision required before |
|---|---|---|---|---|---|---|---|---|---|
| D1 | HTML mockup canvas aspect ratio (≈0.5-0.6) vs. approved PNG aspect ratio (≈0.46-0.56 for mobile, near-match for desktop) | A1,A1-S1,A2,A3,A4,EXTRA-01..04 | `TIER1_HTML_VISUAL_DELTA.md` | Treat PNG aspect as ground truth (taller viewport) | Treat HTML canvas aspect as ground truth | No recommendation — genuine Tier-1 conflict, needs a real target-device decision | Wrong choice skews every subsequent spacing/proportion decision | Do not assume either aspect is "the" target | Wave 6.1 kickoff |
| D2 | Brand accent near-miss: approved `#5A35DF` (117×) vs current `--color-brand-600: #5835DF` | All | `APPROVED_TOKEN_CANDIDATES.md`, `TOKEN_IMPLEMENTATION_AUDIT_V2.md` | Treat as identical (rounding) | Update token to exact approved value | Lean B (low regression risk, 1 token) but not decided here | Very low | Do not silently keep either value as "correct" without a recorded choice | Any token-finalization task |
| D3 | Highest-frequency ink color: approved `#17103A` (188×) vs current `--color-ink-900: #171D3A` | All | Same as D2 | Treat as identical | Update token | Lean B | Low-Medium (affects nearly every text element) | Same | Any token-finalization task |
| D4 | Font family: approved `Noto Sans KR` vs current `Pretendard`/`Black Han Sans`/`Inter` | All | `MEASUREMENT_TABLE_V2.md` §C, `TOKEN_IMPLEMENTATION_AUDIT_V2.md` | Adopt Noto Sans KR to match approved source | Keep Pretendard as an already-made product decision | No recommendation — this reads as a deliberate prior product choice, not an oversight | HIGH if changed (global visual identity) | Do not switch fonts as a side effect of any other Wave 6 task | Before any typography token work |
| D5 | Icon/illustration asset gap: approved PNGs show a distinct Home-hero "blob mascot family" illustration and real flat icons for service tiles / mission rows; none of these exist as files in `screen_renew/uploads/` | A2, A3 | `TIER1_HTML_VISUAL_DELTA.md`, `ASSET_MANIFEST.md` | Commission/source the missing assets before implementing A2/A3 icon-level visuals | Ship with emoji placeholders as an interim, explicitly temporary state | Recommend sourcing real assets before Wave 6.1 A2 work specifically (A2 is currently 0% implemented, so there's no legacy emoji debt to justify shipping placeholders) | Medium (visual credibility) | Do not ship emoji as if it were the approved design | Wave 6.1 A2 kickoff |
| D6 | Room List / DIRECT / SERVICE room kinds exist in current `/wagle` fixture with **no approved-source visual at all** (approved A4 shows only a single GROUP room, mid-conversation) | A4 | `SCREEN_COMPONENT_GAP_MATRIX_V2.md` (`EXTRA_CURRENT_UI`), `APPROVED_SCREEN_INVENTORY.md` | Restrict Wave 6.1 A4 scope to the approved GROUP conversation view only, treat Room List/DIRECT/SERVICE as a separate, later design pass | Treat current fixture's 3-kind scope as already-implicitly-approved and design toward it | No recommendation — this is squarely a product-scope call, not a design-measurement call | Medium (scope creep either direction) | Do not assume the current fixture's scope was ever design-approved just because it exists in code | Wave 6.1 A4 kickoff |
| D7 | Desktop Room auto-selection (`≥701px` auto-selects first room, 1×) and its query-param rewrite behavior | A4 | `DoranLanding.tsx` code comment itself says "§3 PM 정책" (implying a PM decision was expected to exist for this) | Confirm as intended product behavior | Remove/change (e.g. show room list by default on desktop too) | No recommendation — the code already claims PM backing that this task cannot verify the provenance of | Low-Medium (affects every desktop `/wagle` load) | Do not treat this behavior as design-approved on the strength of the approved source alone (the approved source has no desktop chat mockup at all) | Wave 6.1 A4 kickoff |
| D8 | Admin sidebar tone: current legacy dark indigo (`--admin-sidebar-bg: #1e1b4b`) vs approved near-white (`#FBFAFE`) | A5 | `TOKEN_IMPLEMENTATION_AUDIT_V2.md`, `SCREEN_COMPONENT_GAP_MATRIX_V2.md` | Recompose Admin to the approved light theme | Keep dark Admin sidebar as an intentional product differentiator from the family-facing screens | No recommendation | Medium-High (affects every Admin view, not just Point) | Do not silently repaint Admin as a side effect of A5 work without this being its own explicit decision | Wave 6.1 A5 kickoff |
| D9 | A1-S1 (순수 ID/PW 로그인) has no current implementation and no dedicated approved PNG either | A1 | `APPROVED_SCREEN_INVENTORY.md`, `FUNCTIONAL_CONTRACT_MATRIX.md` (`KNOWN_GAP`) | Implement as a new, real login path | Treat as a discarded/alternate design exploration, do not implement | No recommendation — insufficient evidence either way | Low (isolated screen) | Do not build A1-S1 as if it were a confirmed requirement | Before any A1 Wave 6.1 work |
| D10 | Bottom Dock 4th/5th destinations ("나"/individual-tab access to 가족일정/앨범/할일) — code already carries an explicit `PM_DECISION_REQUIRED_BOTTOM_DOCK_ROUTE` comment from a prior pass | A2 | `MongleAppShell.tsx` lines 133-142 (verbatim code comment) | Add dedicated routes for these destinations | Keep them reachable only via the Home ServiceGrid (matches the approved source's own EXTRA-04 Dock-state finding, where "할 일" renders with Home as the active tab, implying it's not a persistent Dock destination in the design either) | Lean B, consistent with independently-confirmed approved-source Dock behavior (`APPROVED_SCREEN_INVENTORY.md` "Notable structural finding") | Low-Medium | Do not add new Dock destinations without resolving this pre-existing, still-open note | Wave 6.1 A2 kickoff |
| D11 | `FAMILY_PLATFORM_DESIGN_IMPLEMENTATION_SOURCE_V1.md` (a prior, separate canonical document, pre-Mongle-rename, referencing an external "PM component contract" on Google Drive not in this task's scope) | Cross-cutting | `STALE_ANALYSIS_REGISTER.md` | Formally supersede/retire this document explicitly in 6.0C | Keep referencing it informally as before | Recommend an explicit decision either way rather than continued informal/undocumented reliance | Low (documentation hygiene) but affects traceability | Do not let a stale-named document remain the *only* documented rationale behind already-shipped tokens/components | Before 6.0C canonical freeze |
| D12 | `docker-compose.phase0.yml` referenced by `tests/e2e/playwright.config.ts` does not exist in this repository | Cross-cutting (E2E infra, not a design question, included because it blocks visual-regression safety net for A1/A3/A5) | `E2E_COVERAGE_MAP_V2.md` | Restore/replace the phase0 stack so the 9 legacy specs can run again | Formally retire `playwright.config.ts`/`specs/` in favor of the newer `playwright.mongle.config.ts`/`specs-mongle/` | No recommendation — infra decision outside this task's design-analysis scope, flagged because it affects how safely Wave 6.1 visual work can be regression-tested | Medium (currently zero regression coverage for A1/A3/A5's legacy routes) | Do not assume the legacy suite currently passes | Before relying on `specs/*.spec.ts` for any Wave 6.1 regression gate |

No item above was resolved by this task. Each traces to a specific file, code comment, or measurement
already produced in Phase A/B.
