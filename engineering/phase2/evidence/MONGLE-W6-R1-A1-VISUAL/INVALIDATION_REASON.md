# Invalidated partial A1 upload

The earlier upload contained implementation screenshots only. It is ineligible
for visual review because canonical captures, side-by-side comparison, pixel
diff, manifest, network audit, and build fingerprint evidence were absent.

The files also violated the capture-dimension contract: the `390x844` file was
390×854 and the `375x812` file was 375×854. They are retained only as an
invalidated partial record, never as final A1 evidence.
