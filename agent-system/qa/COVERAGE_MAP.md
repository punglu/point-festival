# Coverage Map

This is the coverage-status SSOT. Selection, calculation, status definitions,
confidence, lifecycle, tiers, blockers, and failure taxonomy are in
[`TEST_POLICY.md`](TEST_POLICY.md). Commands, environments, secrets, artifacts,
and cleanup are in [`tests/README.md`](../../tests/README.md). This is an index,
not a completion history or a coverage-expansion backlog.

Existing rows were schema-normalized on 2026-07-31 without re-running tests.
Their historic evidence is preserved in Notes. `PARTIAL` is used where the
existing evidence is scoped to a narrower stated behavior; no product-wide
coverage claim is inferred. `NOT_CONFIRMED` means this port did not independently
reinspect the historical assertion and run record. Rows requiring different
full/partial status later must be split.

| ID | Product Area | Feature | Journey | Risk | Lifecycle Target | Coverage Status | Spec/Test Evidence | Last Executed | Execution Result | Confidence | Blocker | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `BE-WEEK-001` | Backend | Week range/classification | Weekly read | Medium | L2 | PARTIAL | `backend/tests/test_weekly.py` | 2026-07-26 | PASS: 6 tests | NOT_CONFIRMED | — | Historic git ref `bcef1a4`; no isolated DB assertions. |
| `API-WEEKLY-001` | API | Weekly authorization/response | Weekly API | High | L2 | PARTIAL | `tests/api/test_weekly_api.sh` | 2026-07-26 | PASS: 6/6, isolated mc_phase0 | NOT_CONFIRMED | — | Historic git ref `c4ab1bf`; defaults to `PHASE0_API_BASE_URL`, not port 8000. |
| `API-SCENARIO-001` | API/DB | Mission/point/notification flows | Scenario flow | High | L3 | PARTIAL | `tests/api/e2e_scenario_test_v2.py` | 2026-07-26 | PASS: 87/87, synthetic runtime | NOT_CONFIRMED | — | Historic git ref `c4ab1bf`; family tenant ownership/idempotency not proven. |
| `E2E-LOGIN-001` | Frontend | Player PIN login | Login | High | L2 | PARTIAL | `tests/e2e/specs/01-login.spec.ts` | 2026-07-26 | PASS, isolated mc_phase0 | NOT_CONFIRMED | — | Historic git ref `e8f597a`; card visibility plus dashboard transition. |
| `E2E-MISSION-001` | Frontend | Mission list/proposal entry | User mission | Medium | L1 | PARTIAL | `tests/e2e/specs/02-mission.spec.ts` | 2026-07-26 | PASS, isolated mc_phase0 | NOT_CONFIRMED | — | Existing assertions are visibility/empty-state and proposal-entry checks. |
| `E2E-ADMIN-001` | Frontend | Admin login/route | Admin entry | High | L2 | PARTIAL | `tests/e2e/specs/03-admin.spec.ts` | 2026-07-26 | PASS, isolated mc_phase0 | NOT_CONFIRMED | — | Historic git ref `e8f597a`; form visibility plus route transition. |
| `E2E-FLOW-001` | Frontend | Player logout/admin access | Login/logout/admin | High | L2 | PARTIAL | `tests/e2e/specs/04-flow.spec.ts` | 2026-07-26 | PASS, isolated mc_phase0 | NOT_CONFIRMED | — | Logout assertion is conditional on visible button; broader state effects unproven. |
| `E2E-VIEWPORT-001` | Frontend | Representative responsive shell | Viewport baseline | Medium | L1 | PARTIAL | `/tmp/phase0-screen-captures` | 2026-07-26 | PASS: 15 PNGs | NOT_CONFIRMED | HUMAN_GATE | Desktop/iPhone/iPad/Android emulation only; physical device/PWA/keyboard/push absent. |
| `API-CURRENT-OWNERSHIP-001` | API | Current ownership/role boundary | Authorization | Critical | L4 | PARTIAL | `agent-system/qa/PHASE0-AUTOMATED-GAP-CLOSEOUT-001.md` | 2026-07-26 | PRODUCT_DEFECT measured | NOT_CONFIRMED | PRODUCT_DEFECT | Anonymous/cross-user mission/point reads and mutations were measured as unsafe; historic ref `163d577`. |
| `API-CHAT-1TO1-001` | API | Pair-participant boundary | Chat access | High | L3 | PARTIAL | `agent-system/qa/PHASE0-AUTOMATED-GAP-CLOSEOUT-001.md` | 2026-07-26 | PASS, isolated route/pair checks | NOT_CONFIRMED | — | Does not claim a Phase 2 room/participant model. |
| `API-LEGACY-CONTAINMENT-001` | API | Legacy default-deny boundary | Authorization | Critical | L4 | PARTIAL | `tests/api/legacy_auth_boundary_test.py` | 2026-07-26 | INDEPENDENT_QA_PASS | NOT_CONFIRMED | — | 91-operation matrix/OpenAPI crosswalk; Phase 1 tenant and Phase 2 room model excluded. |
| `API-PHASE1-RBAC-001` | API/DB | Family RBAC/adapters | Family authorization | Critical | L4 | PARTIAL | `tests/api/phase1_rbac_api_test.py` | 2026-07-26 | INDEPENDENT_QA_PASS, isolated mc_phase1 | NOT_CONFIRMED | HUMAN_GATE | Schema stamp, reviewed bootstrap owner mapping, and production migration remain human-gated. |
| `E2E-NARAN-SHELL-001` | Frontend | Account/family shell and responsive nav | Account entry | High | L2 | PARTIAL | `tests/e2e/specs-mongle/01-shell.spec.ts` | 2026-07-26 | INDEPENDENT_QA_PASS: 25/25 | NOT_CONFIRMED | HUMAN_GATE | Five Chromium-emulated projects; historic naming retained, current paths were updated. **The row ID is a frozen historical identifier** — past QA reports cite it, so it is deliberately not renamed; it is not an active runtime identifier. **The spec it points at was rewritten on 2026-08-01** by `MONGLE-NARAN-RUNTIME-RETIREMENT-AND-FRONTEND-CLOSEOUT-001`; the 25/25 result above predates that rewrite and does **not** cover it — see `E2E-MONGLE-ROUTE-STORAGE-RETIREMENT-001`. |
| `E2E-MONGLE-ROUTE-STORAGE-RETIREMENT-001` | Frontend | Retired-route removal and one-time active-Family storage migration | Shell entry / Family selection | High | L2 | COVERED | `tests/e2e/specs-mongle/01-shell.spec.ts`, `frontend/src/shared/storage/activeFamilyStorageMigration.ts` | 2026-08-01 | PASS: 86 passed / 0 failed / 4 skipped (90 collected, 0 retries), isolated mc_phase1 on a proven current-source image | CONFIRMED | — | Pins the two contracts changed when the retired platform name left the runtime: (1) the retired `/naran/*` paths are **no longer registered** — no redirect, alias or fallback — so they fall through to the 404 page with the URL unchanged, while the canonical `/`, `/dashboard`, `/wagle`, `/family` and the guarded `/admin/*` all still resolve; (2) the persisted active-Family key migrates **once** to `mongle.activeFamily.*` — canonical wins unconditionally, a well-formed *and still accessible* retired value is copied forward before the old key is purged, malformed or inaccessible values are purged without being adopted, a second load changes nothing, and normal Family switching writes the canonical key only. The first independent execution ran against a stale frontend image; its 41 failures are **invalidated** and are not this row's history. Re-executed by `MONGLE-NAMING-CLOSEOUT-E2E-ENVIRONMENT-RECOVERY-001` after the harness was corrected, with the browser proving in-page that it loaded the build made from this worktree. The 4 skips are one desktop-only test declining on the four non-desktop projects. |
| `E2E-HARNESS-CURRENT-SOURCE-GUARD-001` | Agent System / Test Infra | Stale-frontend-image detection for the mc_phase1 E2E harness | E2E bring-up | High | L3 | COVERED | `tests/e2e/scripts/verify-current-source-frontend.sh`, `tests/e2e/scripts/frontend-source-fingerprint.sh`, `tests/e2e/specs-mongle/01-shell.spec.ts` (`Current-source runtime proof`) | 2026-08-01 | PASS positive + PASS negative (planted stale image rejected, EXIT=1) | CONFIRMED | — | Exists because an E2E run reported 41 failures against a frontend image built before the source under test existed, and nothing in the suite could tell. Compares three values that must agree — worktree source fingerprint, the fingerprint labelled into the running image, and the fingerprint the browser fetches from the served app — plus the content-hashed entry bundle name as an independent witness. Also fails if `frontend/.dockerignore` stops excluding `node_modules`, since layering the host darwin-arm64 tree over the container's linux install is how the build environment silently diverges from the lockfile. The guard **recomputes** the worktree fingerprint rather than trusting `MONGLE_FRONTEND_FINGERPRINT`, so a forced env var cannot make it agree with a wrong image. Verified by making it fail, not by reading its PASS output: a deliberately mislabelled image was planted and rejected, then restored. Two stale paths are closed — `--build` in the bring-up script plus `--force-recreate`, and `reuseExistingServer: false` so a leftover container cannot bypass the script entirely. |
| `API-W3-WAGLE-DISPATCHER-001` | API/DB | Outbox realtime dispatcher | Wagle delivery | Critical | L4 | COVERED | `backend/tests/test_wagle_realtime_wave3.py` | 2026-08-01 | PASS: 40/40 (suite 228/228) | CONFIRMED | — | Wave 3. Turns a durable Outbox row into a WebSocket fan-out and a Push attempt, in a transaction separate from the send. **A delivery failure is asserted to leave `wagle_messages` untouched** — the message is durable before any of this runs, which is the whole point of the Outbox. Claim/lease/retry reuse the Wave 2 mechanism rather than adding a second one; an expired lease is proven re-claimable after a mid-delivery crash. Idempotency is tested by running the operation **twice** and asserting the second changed nothing, not by reading a flag. **Found and fixed two latent Wave 2/4 defects**: `claim_batch` ignored `owner_service`, so the SERVICE_ACTION Worker claimed Wagle rows, provisioned a bogus `wagle` ServicePrincipal and drove them to DEAD; and `record_failure` raised `MissingGreenlet` because every caller reaches it right after a rollback expires the instance, so the retry handler itself failed. Both fixes touch graduated Wave 2 code and were authored by the session that found them — independent QA should read them critically. |
| `API-W3-WAGLE-WEBSOCKET-001` | API | Account-native realtime gateway | Wagle realtime | Critical | L4 | PARTIAL | `backend/tests/test_wagle_websocket_gateway_wave3.py` | 2026-08-01 | PASS: 15/15 | CONFIRMED | — | Wave 3. Drives the **real** `wagle_realtime_socket` coroutine — same auth, subscribe/resume loop and registry — against a scripted socket; only network framing is stubbed, because `TestClient` runs the app in a different event loop from the asyncpg connections. Browser framing is covered by `E2E-MONGLE-WAGLE-REALTIME-001` instead, so this row is `PARTIAL` rather than claiming full coverage. Denies: no token, **legacy player JWT**, revoked Session, suspended Account, cross-family room, spoofed `family_id`. Allows and asserts: multiple physical sockets per Session (the Realtime Contract forbids an invariant against them), one socket holding subscriptions in several Families, and **asymmetric revocation** — Session revoked closes everything, one Family's membership ending drops only that Family. Resume is asserted to return missed events in room-sequence order, to page rather than truncate, and to refuse history from before a participant joined. |
| `API-W3-WAGLE-PUSH-PIN-001` | API/DB | Push subscription lifecycle and device PIN | Wagle notification / device lock | Critical | L4 | COVERED | `backend/tests/test_wagle_realtime_wave3.py` | 2026-08-01 | PASS: within 40/40 | CONFIRMED | — | Wave 3 (migration `0009`). Push: Account+device scoped (never Family-duplicated), re-registering the same endpoint under another Account revokes the first (the Account-switch case), 410 expires only that subscription while a transient failure does not, delivery is idempotent per `(outbox_event_id, subscription_id)`, and the API never echoes the endpoint or key material — together they are a capability to push to that browser. **No external push service is contacted in any test.** PIN: bcrypt via the Account credential path, attempt-limited with a temporary lock that a correct PIN cannot bypass, reset-not-retrieval (no `get_pin` function exists at all), per-device attempt isolation, `pin_version` bumped on reset so a stale client unlock cannot survive, not Family-scoped, and **does not reuse the legacy player `pin_hash`**. A lockout is asserted to leave the Account active and Push subscriptions intact — a screen lock must never become a platform outage. **Push payload is identifiers-only pending `D6-P1`**, asserted so a fixture body cannot become the shipped default. |
| `API-W3-WAGLE-MULTIWORKER-FANOUT-001` | API/Infra | Cross-worker realtime fan-out via PostgreSQL LISTEN/NOTIFY | Wagle realtime under `--workers 2` | Critical | L4 | COVERED | `backend/tests/test_wagle_multiworker_fanout.py`, `backend/app/domains/wagle/realtime_notify.py` | 2026-08-01 | PASS: 10/10 (suite 238/238) + runtime `--workers 2` check | CONFIRMED | — | Wave 3, PM-approved mechanism (no Redis/Kafka/NATS — the database this system already runs is the channel). **NOTIFY is a wake-up signal, never the message**: the payload's exact key set is pinned by test and has no body field, and PostgreSQL's 8000-byte NOTIFY cap means a content-carrying design would fail on precisely the longest messages. Asserted: an event published on worker A reaches a connection held by worker B (A has no local subscriber, so everything B received crossed PostgreSQL); the publishing worker does not deliver its own event twice (`origin_id`); duplicates carry identical `message_id`/`room_sequence` so client dedup holds; **a missed notification is still recovered with no NOTIFY emitted at all** — the durable cursor fallback is preserved, not replaced; a killed listener reconnects and resumes without replaying; a publish failure never suppresses local delivery; and subscribers in another Family or room are not woken. Runtime evidence is reported separately and deliberately: a real `uvicorn --workers 2` container showed 2 `app.main` processes, 2 backends holding `LISTEN "wagle_realtime"`, and an HTTP 101 upgrade — which proves per-worker listener attachment, while the delivery behaviour itself is proven by the tests. A single test process cannot reproduce OS-level process isolation, so the two are not merged into one claim. |
| `E2E-MONGLE-WAGLE-REALTIME-001` | Frontend | Wave 3 browser slice: realtime lifecycle, PIN gate, service worker | Wagle entry | High | L2 | PARTIAL | `tests/e2e/specs-mongle/02-wagle-realtime.spec.ts` | 2026-08-01 | PASS: 121 passed / 0 failed / 4 skipped (125 collected, 0 retries), isolated mc_phase1 on a fingerprint-verified image | CONFIRMED | — | Wave 3. Covers what actually shipped to the browser: the realtime connection settles into `authorization_lost` for a legacy-player session (the gateway is Account-native by D3) **without a reconnect storm**, an optional-feature 401 does not end the session or hide the screen, the PIN gate never blocks Markpoint or the Family screens, no PIN endpoint leaks a hash or attempt counter, and `sw.js` renders no message content. `PARTIAL` and explicitly bounded: a message-level "send here → receive there" journey is **not** covered because the Wagle screen still renders preview fixtures (Wave 6 `MONGLE-W5-TARGET-UI-001`), and browser Web Push is **not run** at all — it needs VAPID keys and a real push service. Neither is reported as PASS. |
| `API-W5-MARKPOINT-CORE-001` | API/DB | Markpoint cycle config, rolling materialization, projection, deduction correction, level, admin bulk | Markpoint family operations | Critical | L4 | COVERED | `backend/tests/test_markpoint_core_gap_wave5.py` | 2026-08-01 | PASS: 54/54 (suite 312/312) | CONFIRMED | — | Wave 5 Core closeout (migration `0011`). Closes seven Coverage Matrix rows. **Cycle config is per Family, not the legacy global row** — one Family's change cannot move another's period boundaries, asserted from the second Family's side. Guard A blocks a change while the current period runs (the legacy `validate_cycle_change` rule, re-expressed per Family); Guard B blocks it while an ACTIVE Template exists, scoped to that Family's own templates; a non-cycle label is deliberately not blocked; **no force override exists** and a test asserts the module exposes none. Rolling materialization preserves `get_rolling_window` **as implemented** — its own docstring contradicts its code for a Monday (7 days claimed, 14 produced) and the code was kept, with the contradiction raised as a PM item rather than silently resolved. Only the Template's named assignee is targeted, never every ACTIVE membership; duplicate prevention is a DB unique constraint plus a SAVEPOINT retry, proven under `asyncio.gather`. Deduction correction is two new rows (reversal + optional replacement) and the **original row is re-read and asserted unchanged**; append-only is additionally enforced by DB triggers, discovered when a test's own `UPDATE` was refused. Level input is `lifetime_earned`, not balance — spending must not lower a level — with a 10-case boundary matrix including the exact-last-tier branch that keeps its curated title. Bulk approval is all-or-nothing, and the test asserts the *eligible* mission was also left unpaid. |
| `API-W5-MARKPOINT-HTTP-AUTHZ-001` | API | Markpoint HTTP authorization matrix | Markpoint access control | Critical | L4 | COVERED | `backend/tests/test_markpoint_http_authorization_wave5.py` | 2026-08-01 | PASS: 15/15 | CONFIRMED | — | Wave 5. Run over **real HTTP requests**, not service calls: a service test proves the service checks something, it cannot prove the route is wired to that check, and a forgotten dependency lives in the wiring. Actors: no Session, revoked Session (401 immediately, not at token expiry), suspended Account, suspended Membership, cross-family ServiceAdmin, inactive subscription, ACTIVE access restriction (blocks that member only — the unrestricted member in the same Family still gets 200), plain member, **family owner** (403 on every admin capability — D4, and the reason migration `0006` stripped the permission from every FAMILY-scope role), mission-manager vs point-admin separation in both directions, and the positive case that makes the negatives meaningful. A foreign mission id returns **404 rather than 403**, because a 403 would confirm the id exists in another Family. |
| `AGENT-CLOSEOUT-001` | Agent System | Closeout synchronization checker | Task closeout | Medium | L2 | PARTIAL | `agent-system/tools/check_closeout.py` | 2026-07-26 | INDEPENDENT_QA_PASS | NOT_CONFIRMED | — | Permanent stdlib regression test exists; checker remains report-only. |
| `AGENT-CLOSEOUT-002` | Agent System | Open/archive lifecycle detection | Task closeout | Medium | L2 | PARTIAL | `agent-system/tools/check_closeout.py` | 2026-07-26 | INDEPENDENT_QA_PASS | NOT_CONFIRMED | — | Historic evidence confirms mixed/missing/duplicate/stale-relay detection. |
| `AGENT-CLOSEOUT-003` | Agent System | Closeout regression matrix | Task closeout | Medium | L2 | PARTIAL | `agent-system/tools/check_closeout.py` | 2026-07-26 | PM_ACCEPTED | NOT_CONFIRMED | — | Historical 23/23 external evidence; permanent matrix has its own count. |
| `E2E-MONGLE-WAVE6-1-001` | Frontend | Foundation visual/responsive regression | Mongle shell | High | L2 | PARTIAL | `tests/e2e/playwright.mongle.config.ts` | 2026-07-31 | INDEPENDENT_QA_PASS: 66/0/4 ×2 | NOT_CONFIRMED | HUMAN_GATE | Five projects; visual delta 0.000–0.022%, dynamic seed timestamp; physical devices remain human gate. |
| `API-W1-ACCOUNT-AUTH-001` | API/DB | Account credential, persistent Session, device unlink | Account login/session | Critical | L4 | COVERED | `backend/tests/test_account_auth_wave1.py` | 2026-08-01 | PASS: 33/33 (suite total 104/104) | CONFIRMED | — | Wave 1. Login with zero legacy identity, lockout, refresh rotation with old-token replay denial, revoked-session rejection while the access token is still unexpired, per-device unlink, password-change session invalidation. Executed against a disposable volume-less Postgres on 15435 (`docker-compose.phase2.yml` is absent from the repo). Password/refresh-token secrets asserted to exist only as hashes. **Independently re-verified 2026-08-01** (`MONGLE-W1-INDEPENDENT-QA-001`): 104/104 reproduced from a freshly created disposable Postgres 16.9 container (`database/init.sql` baseline + `alembic upgrade head`), 33/33 confirmed in isolation; see `agent-system/qa/MONGLE-W1-INDEPENDENT-QA-001.md`. |
| `API-W1-SCOPED-RBAC-001` | API/DB | Membership-scoped RBAC and FamilyAdmin/ServiceAdmin separation | Family authorization | Critical | L4 | COVERED | `backend/tests/test_account_auth_wave1.py` | 2026-08-01 | PASS: 33/33 | CONFIRMED | — | Wave 1. Default deny, cross-family denial, suspended-membership denial, last-owner protection, role revocation on membership termination, issuer audit. **Found and fixed a real contract violation**: the `0001` seed auto-granted `markpoint.missions.manage`/`markpoint.points.adjust` to FAMILY `owner`/`admin`, violating D4's no-automatic-ServiceAdmin rule; removed by migration `0006`. Zero current product impact (no route reads those codes yet) — it would have activated at the Wave 4/5 Markpoint authorization transform. **Independently re-verified 2026-08-01** (`MONGLE-W1-INDEPENDENT-QA-001`): fresh `0000`→`0006` upgrade, `downgrade -1`/re-upgrade and full `0004` downgrade/re-upgrade all confirmed by direct SQL (role_permissions/permissions rows), zero duplicate or residual rows; see `agent-system/qa/MONGLE-W1-INDEPENDENT-QA-001.md`. |
| `API-W2-WAGLE-DURABLE-001` | API/DB | Wagle durable message + transactional Outbox | Wagle send | Critical | L4 | COVERED | `backend/tests/test_wagle_durable_wave2.py` | 2026-08-01 | PASS: 20/20 (suite 170/170) | CONFIRMED | — | Wave 2. Message row, room-sequence advance and Outbox append commit as one transaction; a forced post-persist failure is proven to roll back all three, checked from a second session. Table-wide invariant asserted (every message has exactly one event, and vice versa). Idempotent retry returns the same message with no second event; same `client_message_id` with a different body is 409. Executed against a disposable volume-less Postgres. Outbox **consumption** is Wave 3 and is deliberately not covered. **Naming corrected 2026-08-01 (PM):** the event emits `owner_service="wagle"` / `wagle.message.created` / `wagle_message`; an earlier revision emitted the historical name and was rejected. A regression test asserts over the stored rows that no emitted identifier carries it. Test paths moved to `wagle_*` by migration `0007`. |
| `API-W2-WAGLE-ORDERING-READ-001` | API/DB | Room-scoped ordering, cursor and read state | Wagle history | High | L3 | COVERED | `backend/tests/test_wagle_durable_wave2.py` | 2026-08-01 | PASS: 20/20 (suite 170/170) | CONFIRMED | — | Wave 2. Sequence monotonic and gapless per room; two rooms proven to hold independent sequence spaces, which is what rules out an accidental global counter. Forward-only read cursor (a backward PUT is accepted but does not regress the stored value); per-membership read isolation; out-of-range cursor rejected 422. Cross-family read and write both denied. These behaviours pre-existed Wave 2 and are pinned here rather than rebuilt. |
| `API-W2-WAGLE-ROOM-SUMMARY-001` | API/DB | Batch room list with preview and unread | Wagle room list | Medium | L2 | COVERED | `backend/tests/test_wagle_durable_wave2.py` | 2026-08-01 | PASS: 20/20 (suite 170/170) | CONFIRMED | — | Wave 2. New `/room-summaries` returns each room with last-message preview and unread count in one round trip, closing an N+1. Own messages excluded from unread; a deleted last message keeps its slot but returns no body; rooms the caller does not participate in are absent; family isolation verified; an empty room lists with a null preview. Known inherited semantic, not a defect of this row: because `sender_participant_id <> :pid` is NULL for SERVICE_ACTION rows, service messages raise no unread — matched to `read_state()` deliberately so list and detail agree; the user-visible rule is D6-P4, still undecided. |
| `API-FE-SERVICE-CODE-CONTRACT-001` | API/Frontend | Frontend service-code contract | Wagle availability | High | L3 | COVERED | `backend/tests/test_frontend_service_code_contract.py` | 2026-08-01 | PASS: 3/3 (suite 173/173) | CONFIRMED | — | Pins the seam where the Wave 2/4 rename broke the UI: the frontend compares `service_code` from a live `/api/account-context` response against a literal, and migration `0007` renamed that value. A type check cannot catch it — both sides are `string`. Asserts the API emits exactly `wagle`, that the historical code appears nowhere in the payload, and that the stored subscription row carries the Target code, so a future rename of either side fails a test instead of silently blanking the screen. |
| `API-INTEG-WAGLE-NAMING-001` | API/DB | Wagle runtime identifier migration | Naming contract | High | L3 | COVERED | `backend/tests/test_integration_wagle_markpoint.py` | 2026-08-01 | PASS: 25/25 (suite 170/170) | CONFIRMED | — | Integration. Migration `0007` renamed tables, indexes, constraints, trigger functions, permission codes, service code and route prefix from the historical name to Wagle in one step. Asserted against the live catalog (`pg_tables`/`pg_indexes`/`pg_constraint`/`pg_proc`/`pg_trigger`/`permissions`/`roles` all zero for the historical prefix), plus 404 on the old route, the new route answering, and OpenAPI publishing only `/wagle/`. `role_permissions` bindings survive because codes are renamed in place. Verified upgrade → downgrade → re-upgrade with zero residue. |
| `API-INTEG-MARKPOINT-ACCESS-001` | API/DB | Markpoint activation lifecycle and restriction | Service access | Critical | L4 | COVERED | `backend/tests/test_integration_wagle_markpoint.py` | 2026-08-01 | PASS: 25/25 | CONFIRMED | — | Integration (migration `0008`). Member request → FamilyAdmin approve/reject; one PENDING request per family (idempotent for the same requester, 409 for another); reprocessing a decided request denied; plain member cannot approve; cross-family approval denied both directions; direct activation idempotent and grants no role; per-member restriction removes default access and restore returns it; restriction idempotent with history kept; cross-family restriction denied; **no MarkpointParticipant table and no Mission row is created by access control**. |
| `API-INTEG-MARKPOINT-WAGLE-RELAY-001` | API/DB | Markpoint → Wagle system-actor relay | Cross-domain event | High | L3 | COVERED | `backend/tests/test_integration_wagle_markpoint.py` | 2026-08-01 | PASS: 25/25 | CONFIRMED | — | Integration. Relay publishes as the `ServicePrincipal` with `sender_participant_id` NULL (a service can never post under a human name); dedup on repeat source event leaves one message; unapproved binding, cross-family binding and mismatched principal all rejected; envelope validation refuses a missing actor/binding. **Boundary asserted against the source itself**: no file under `app/domains/markpoint_access/` imports a Wagle model — the relay goes port → adapter → `wagle.service`. |
| `API-W1-FAMILYADMIN-ISSUANCE-001` | API/DB | FamilyAdmin provisions an independent member Account | Account provisioning | Critical | L4 | COVERED | `backend/tests/test_account_auth_wave1.py` | 2026-08-01 | PASS: 33/33 | CONFIRMED | — | Wave 1. Own-family-only issuance, cross-family denial, non-admin denial, duplicate-username whole-unit rollback (no stranded Account/Membership), one-time initial password stored only as a bcrypt hash, issuer recorded, no impersonation path and no post-hoc password read. **Independently re-verified 2026-08-01** (`MONGLE-W1-INDEPENDENT-QA-001`); see `agent-system/qa/MONGLE-W1-INDEPENDENT-QA-001.md`. |
| `API-W6-BG1-CREDENTIAL-SURFACE-001` | API | Dual-credential resolution on the legacy-shaped auth dependency | Account/legacy credential boundary | Critical | L4 | PARTIAL | `backend/tests/test_bg1_credential_surface_unification.py` | 2026-08-01 | Independent re-QA: 6/6 twice on separately initialized disposable DBs after `78913b4`; malformed `sid` returns 401 through both entry points. Direct malformed role/claim probes also failed closed. Complete independent full suite did not finish (stopped after 32 tests), so no full-regression count is claimed. | CONDITIONAL | — | The correction is independently verified at its boundary, but a complete independent backend regression remains required. Underlying fix: `get_current_user` widened `{player, admin}` → `{player, admin, account}` and both Account entry points use `auth_service.resolve_account_from_session_claim`. |
| `API-W7-5-PROFILE-MISSION-WAGLE-EXT-001` | API/DB | Self-service profile fields, self-service Membership relationship, Mission description/checklist/rejection-reason/reviewer-name, Wagle message reply-to | W7.5 Phase C data wiring | Medium | L3 | COVERED | `backend/tests/test_w75_phase_c_extensions.py` | 2026-08-03 | PASS: 15/15 (full suite 332/333, see `KNOWN-W7-5-WAGLE-CONCURRENCY-001`/`UNRELATED_FAILURE` note below for the one non-matching test) | CONFIRMED | — | `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`. All additive: migration `0012_profile_mission_fields` (nullable `accounts.bio`/`birthday`/`avatar_color`, nullable `markpoint_missions.description`/`.checklist`), no column altered, no backfill. Covers `PATCH /api/me` (partial-update semantics, invalid-avatar-color 422), self-service `PATCH /api/families/{id}/members/me` (no `FAMILY_MEMBERS_MANAGE` required, unknown-relationship 422, cross-family 403), Mission checklist-toggle authorization (wrong-assignee 404, post-submission 409), and Wagle reply-to's cross-room-forgery rejection (404). 4 companion Playwright cases in `tests/e2e/specs-mongle/04-w75-data-wiring.spec.ts` (`1f`/`1k`/`2g`) pass against an isolated stack. |
| `API-W7-5-PHASE-D-NEW-SLICES-001` | API/DB | 10 new vertical Slices: Todo, Family Rules, Notification Preferences, Schedule, Album metadata, Reward Catalog + self-service redemption, Account-native Notification list, Family Activity Log (reads existing Markpoint audit), Wagle-board-reuse (posts/comments via existing Wagle messages), cross-entity Search (Missions + Wagle messages) | W7.5 Phase D data wiring | Medium | L3 | COVERED | `backend/tests/test_w75_phase_d_slices.py` | 2026-08-03 | PASS: 34/34 (full suite see `KNOWN-W7-5-WAGLE-CONCURRENCY-001`/`UNRELATED_FAILURE` notes for non-matching tests) | CONFIRMED | — | `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`. 7 new additive migrations (`0013`→`0019`), 2 Slices with no migration (Activity Log reads `MarkpointAuditEvent`; Search reads Missions + Wagle messages; Wagle-board-reuse reuses `WagleRoom`/`WagleMessage` with a body-encoding convention, zero schema change). New `markpoint_target.service.self_spend` gives Reward redemption a self-service point-debit path requiring only `require_access`, not the admin-only `POINTS_ADJUST` `adjust_points` needs. `3c`/`3d` (Wagle board post/comment) live-verified in a real Chromium session — see `E2E-W7-5-WAGLE-BOARD-001` below for the now-permanent spec. `SLICE-WAGLE-ATTACHMENTS` (`2b`) is not covered here — correctly not built, `POLICY_BLOCKED` on the storage-infra decision (see `engineering/phase2/MONGLE_W7_5_PM_DECISION_PACKAGE.md` §5). `SLICE-WAGLE-BOARD-REACTIONS` (`3e`) is covered separately — see `API-W7-5-BOARD-REACTIONS-001` below; it was resolved and built in a later checkpoint of this same task, not left unbuilt. **Correction (Phase G):** this row's backend/API coverage for `SLICE-NOTIFICATION-LIST` (`1n`) was always real, but the Handoff's separate "Fully real end-to-end" frontend tally wrongly counted `1n` in that list at the time it was written — `frontend/src/features/family-notifications/NotificationsPage.tsx` was still calling nothing real (`() => undefined` handlers, fixture-only render) until a later checkpoint's frontend audit found and fixed it. Backend/API coverage in this row was never wrong; only the separate frontend-completeness claim was — see this task's QA evidence "Phase G" section for full detail. |
| `API-W7-5-BOARD-REACTIONS-001` | API/DB | Wagle message reactions (toggle) + popularity ranking (reaction_count + comment_count, week/month/all) | W7.5 Phase D/E, `3e` (인기 게시글) | Low | L3 | COVERED | `backend/tests/test_w75_phase_d_board_reactions.py` | 2026-08-03 | PASS: 15/15 (full suite: run 12 in `KNOWN-W7-5-WAGLE-CONCURRENCY-001`, 382/382, 0 failed) | CONFIRMED | — | `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`. Resolved as `IMPLEMENTATION_REQUIRED` rather than deferred to PM: both `3c` and `3e`'s frozen canonical Screens already render `♥ likes · 💬 comments`, so this makes an already-designed-for stat real. One additive migration (`0020_wagle_message_reactions`, toggle semantics via a `(message_id, reactor_membership_id)` unique constraint, `ON DELETE CASCADE` from `wagle_messages`). `MessageResponse` gained additive `reaction_count`/`reacted_by_me` fields (existing 4 call sites of `message_out()` unaffected — only the list endpoint batches real counts, the other 3 default to `0`/`false`, unchanged behavior). Real counts wired into `3c`'s post list and `3e`'s ranked list. The reaction *toggle* itself has no click target in either frozen Screen's own type contract (`FamilyBoardProps`/`PopularPostsProps` neither declares a like/react callback) — `DESIGN_CONTRACT_REQUIRED`, tracked as GATE list item in the PM Decision Package, not fabricated as done. **`MONGLE-W7-5-INDEPENDENT-QA-REMEDIATION-001` (F1)**: `list_popular_posts`'s `range=week`/`month` branches were found to 500 (`asyncpg.exceptions.DataError`, an int bound into a `text || 'days'` interval expression with no int/text operator overload) — every test in this file before that remediation used `range="all"` or an empty board room, both of which bypass that exact code path. Fixed (`:days * INTERVAL '1 day'`, keeps `days` a genuine bound parameter) and 7 new tests added specifically exercising the previously-untested branch, proven to be real regression coverage by a revert-and-reconfirm round trip (5 failed with the exact original error when the fix was reverted). |
| `E2E-W7-5-WAGLE-BOARD-001` | Frontend | `3c`/`3d`/`3e` real board post, real comment, real reaction ranking | W7.5 Phase E, board/comment/popular journey | Medium | L2 | COVERED | `tests/e2e/specs-mongle/04-w75-data-wiring.spec.ts` (`3c/3d/3e` describe block) | 2026-08-03 | RUN 1 FAILED (test defect), RUN 2 PASS: 1/1 against an isolated stack (Postgres 15435, backend 18099, Vite 5199) | CONFIRMED | — | `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`. Replaces an earlier ad hoc verification script that was run once and deleted — per this task's own reconciliation pass, that was corrected: this spec is now a permanent, reproducible, committed artifact, not a one-time scratch script. Seeds a real board Room + post + comment directly over the already-real Wagle HTTP API (the frozen `3c` Screen has no compose form to create a post from), then asserts the real post/comment render through the product UI and that a real reaction changes the ranked Popular Posts result. **First execution genuinely failed** (30s timeout on `page.goBack()` losing the board view, since `WagleBoardPage.tsx`'s post/comment view is local component state, not a route) — a real test defect, not a product defect. Fixed by clicking the comment composer's own `←`/`onCancel` control instead of browser history. Re-seeding for the re-run also exposed a second real (pre-existing, latent) defect: `backend/scripts/phase1_seed_synthetic.py` deleted `WagleParticipant` before `WagleMessage`/`WagleParticipantReadState`, violating the `ON DELETE RESTRICT` FK once a real Wagle message existed in that seed DB for the first time — fixed by reordering the delete list. Full root-cause detail in `agent-system/qa/MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001.md`'s "Phase F" section. |
| `E2E-W7-5-FULL-SPEC-001` | Frontend | Full permanent spec: `1a-1`×2, `1r`, `1q`, `1t`, `1f`, `1k`, `2g`, `2t`, `3c/3d/3e` | W7.5 full data-wiring regression | Medium | L2 | COVERED | `tests/e2e/specs-mongle/04-w75-data-wiring.spec.ts` (all describe blocks) | 2026-08-03 | PASS: 10/10, 0 skipped, 0 failed (16.2s) against an isolated stack (Postgres 15499, backend 18098, Vite 5198) | CONFIRMED | — | `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`, Phase H. Previously `2t` was `ENVIRONMENT_REQUIRED`-shaped-skip pending `MONGLE_W75_ADMIN_PASSWORD` — resolved this checkpoint by generating a synthetic, disposable-only password, writing its bcrypt hash directly into the throwaway DB's pre-existing seeded `admin_auth` row for username `dad` (`database/init.sql`'s own synthetic seed), and exporting the env var for one run only; the password was never written to any file, document, or committed artifact, and the throwaway stack was torn down immediately after. This is the first unconditional, full, 0-skip execution of the entire permanent spec in this task's history. **`MONGLE-W7-5-INDEPENDENT-QA-REMEDIATION-001` (F2)**: that manual per-checkpoint procedure is now a permanent, no-manual-steps script, `tests/e2e/scripts/run-w75-full-spec.sh` (documented in `tests/README.md`), so Independent QA can reproduce this exact 10/10 result without being told the credential-setup steps out of band. Re-verified 2 consecutive times via the script itself: 10/10 both times. **(F1)**: the `3c/3d/3e` test's own assertion was strengthened after a stale-DOM false-positive was found — it previously asserted page-wide visible text, which would have passed even if the Popular Posts fetch 500'd, since the board's own post list stays mounted underneath that overlay. Now asserts the real network response (`waitForResponse`, status 200, payload contains the created post's `message_id`) before checking text scoped to the overlay's own `data-testid`. A second, unrelated, genuinely new defect surfaced by this stricter assertion — a board-room creation race between the app's own mount effect and this test's own identical find-or-create script, producing two rows sharing the `__family_board__` sentinel title on a brand-new Family — was fixed at the test level only (`page.waitForLoadState('networkidle')` before the test's own room lookup); the same race in real concurrent multi-device usage is disclosed, not fixed (would need a new migration/unique constraint, out of this remediation's stated scope). |
| `KNOWN-W7-5-WAGLE-CONCURRENCY-001` | API/DB | Full backend-suite non-determinism confined to Wagle's own concurrency-testing files | Full-suite regression | Medium | L3 | KNOWN_CONDITION | `backend/tests/test_wagle_realtime_wave3.py`, `test_wagle_reliable_service_slice.py`, `test_wagle_integration.py`, `test_wagle_durable_wave2.py` | 2026-08-03 | **13** independent full-suite runs across `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`'s eight checkpoints: run 1 309/318 (Phase B), run 2 318/318 (Phase B clean re-run), run 3 316/318 (Phase B/C `1t`), run 4 318/318 (`1t` clean re-run), run 5 332/333 (Phase C closeout), run 6 361/365(+2 errored)/1 failed (Phase D, concurrent with a synthetic-seed script on the same DB), run 7 365/366/1 failed (Phase D, immediate clean re-run), run 8 365/369(+3 errored)/1 failed (Phase D, final closeout run), run 9 374/375/1 failed, 0 errors (Phase E scope-reconciliation checkpoint), run 10 **375/375, 0 failed, 0 errors** (Phase H, immediately after the `bcrypt` fix below — the first fully clean run of the entire task), run 11 **375/375, 0 failed, 0 errors** (Phase H, after the additional cross-domain-boundary refactor, confirming both fixes stack with zero regression), run 12 **382/382, 0 failed, 0 errors** (Phase I remediation, F3 first of 2 consecutive runs, post-F1-fix — test count rose from 375 to 382 because F1 added 7 new regression tests), run 13 **382/382, 0 failed, 0 errors** (Phase I, F3 second consecutive run, identical result), run 14 **389/389, 0 failed, 0 errors** and run 15 **389/389, 0 failed, 0 errors** (`MONGLE-W7-5-BOARD-ROOM-RACE-AND-AUTH-HARDENING-001`, two consecutive runs post-fix — test count rose from 382 to 389 because this task added migration `0021`'s own concurrency regression test plus 6 new admin-login-bcrypt tests), run 16 **399/399, 0 failed, 0 errors** and run 17 **399/399, 0 failed, 0 errors** (`MONGLE-W7-5-HARDENING-QA-FAIL-REMEDIATION-001`, two consecutive runs post-fix — test count rose from 389 to 399 because this task added the 10-case migration-0021 participant-merge regression matrix). Full exact detail — command, HEAD, timestamp, exit code — in `agent-system/qa/MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001.md`'s own "Exact test evidence"/Phase H sections. Standalone/isolated re-runs of **every** implicated test across all checkpoints: 100% pass every time — none has ever failed twice. | CONFIRMED (condition), not PASS | KNOWN_CONDITION | Formal registration per PM direction and `TEST_POLICY.md`'s failure taxonomy: a sub-100% full-suite result is never recorded as PASS — runs 1-9 were all sub-100% for this reason. Different specific tests fail on different runs (`test_ordering_is_per_room_not_global`, `test_11_cursor_pagination_no_duplicates_no_gaps`/`test_12a_left_participant_visibility_capped_and_write_denied` recurring, others seen earlier and never again), always confined to Wagle's own `asyncio.gather`-driven concurrency tests (more exposed to real host-level timing variance than the rest of the suite), with one run showing an explicit `DeadlockDetectedError` on a fixture-teardown `TRUNCATE TABLE` — conclusively non-deterministic infrastructure-level contention, not a deterministic code defect, and not caused by any of this task's own changes across any phase (all touch sequential reads, not the concurrent-write paths that fail). Matches the identical shape already documented at `API-W6-BG1-CREDENTIAL-SURFACE-001`'s task history. **Not resolved by runs 10-11's clean results** — two clean runs narrow but do not retire a condition already documented as intermittent across 9 prior runs; stabilizing the actual contention (e.g. serializing the concurrency-test files, or widening the fixture-teardown lock window) remains separate, not-yet-scheduled work, and is a precondition for declaring this row's own condition closed. **Correction (Phase H): the separate `test_wagle_service_binding.py::test_01_user_jwt_blocked_from_service_ingress` failure (`bcrypt` 72-byte-limit crash on a legacy-JWT-as-service-secret) that this Notes field previously said was "not fixed by `MONGLE-W7-5-DATA-AND-BEHAVIOR-WIRING-001`" WAS investigated and safely fixed by this task in its Phase H checkpoint** — root cause: `get_current_service_principal` called `bcrypt.checkpw` on an oversized secret without a length guard; fixed by rejecting any secret over bcrypt's 72-byte limit before calling it, in `backend/app/domains/wagle/service_actor.py`. This was always a deterministic, non-concurrency-shaped failure (correctly never folded into this row's own `KNOWN_CONDITION`) — its own separate fix is why runs 10-11 above no longer show it. |
| `API-W7-5-BOARD-ROOM-RACE-HARDENING-001` | Backend | Family-board GROUP-room duplicate-creation race, closed via DB invariant + atomic get-or-create | Board first-visit concurrency | High | L3 | COVERED | `backend/tests/test_wagle_integration.py::test_02b_family_board_concurrent_creation_converges_on_one_room`, `backend/alembic/versions/0021_board_room_race_hardening.py` | 2026-08-03 | PASS: 5 concurrent requests x 10 fresh Families, every iteration converges on one room_id, exactly 1 surviving row per family confirmed by direct count | CONFIRMED | — | `MONGLE-W7-5-BOARD-ROOM-RACE-AND-AUTH-HARDENING-001`, fixing `RE-QA-F-BOARD-ROOM-RACE` (HIGH) from `MONGLE-W7-5-FOCUSED-INDEPENDENT-RE-QA-001` (10/10 concurrency iterations previously produced duplicate rooms with 0 errors, entirely silent). Two parts: (1) migration `0021` adds a partial unique index (`uq_wagle_rooms_family_board_singleton`, scoped to the reserved `__family_board__` sentinel only — ordinary GROUP rooms may still share a title) plus a lossless duplicate-merge routine, independently verified against synthetic seeded duplicates (3 rooms, overlapping/non-overlapping memberships, cross-room messages, reactions, read-states, plus an untouched negative-control Family) in a dedicated disposable DB: all 4 messages/2 memberships/2 reactions survived, downgrade cleanly dropped only the index (data stayed merged), re-upgrade recreated the index with 0 errors; (2) `create_room`'s GROUP branch now does a pre-check + `IntegrityError`/rollback/refetch get-or-create for the reserved title only, mirroring the existing DIRECT-branch pattern. Live persistent dev DB (`mongle-db-1`, read-only observation only) held 0 existing duplicates at investigation time — the merge migration is a no-op there but still required and independently exercised, not skipped because production happens to be clean today. **Correction (`MONGLE-W7-5-HARDENING-QA-FAIL-REMEDIATION-001`, 2026-08-03, remediating `HARDENING-QA-F-001` FAIL from `MONGLE-W7-5-HARDENING-FOCUSED-INDEPENDENT-QA-001`):** the merge's participant-survivor selection had a real defect — it picked among loser-room rows only, ordered by `joined_at` with no `status` filter, so a genuinely `active` loser row could lose to an older `left` row already in canonical (independently confirmed: a throwaway copy of the pre-fix algorithm, run against the exact reported shape and rolled back, reproduced `{left: 2, active: 0}` verbatim). Rewritten to consider every row across the whole duplicate set — an active row anywhere always wins. Read-state folding was also corrected from a raw `GREATEST` on pre-merge sequence numbers (meaningless after a global renumber) to an explicit old-to-new sequence translation. See `API-W7-5-MIGRATION-0021-PARTICIPANT-MERGE-MATRIX-001` below for the new permanent regression coverage. Re-verified lossless + round-trip on a fresh disposable DB with the exact bug shape: fixed, confirmed. |
| `API-W7-5-ADMIN-LOGIN-BCRYPT-HARDENING-001` | Backend | Legacy admin login bcrypt 500 on oversized password, closed with fail-closed guard | Admin authentication | Medium | L2 | COVERED | `backend/tests/test_auth_admin_login.py` (6 tests) | 2026-08-03 | PASS: 6/6 — oversized ASCII/multi-byte password both 401 not 500, unknown-username oversized password 401, correct/incorrect normal-length password unchanged, schema still accepts a long password | CONFIRMED | — | `MONGLE-W7-5-BOARD-ROOM-RACE-AND-AUTH-HARDENING-001`, fixing `RE-QA-F-ADMIN-LOGIN-BCRYPT` (MEDIUM) from `MONGLE-W7-5-FOCUSED-INDEPENDENT-RE-QA-001` — `authenticate_admin` called `bcrypt.checkpw` with no length guard, so any unauthenticated caller who knew/guessed a valid admin username could trigger a 500 with a >72-byte password. Fixed with the same fail-closed `try/except (ValueError, TypeError): return False` pattern `family/auth_service.py::verify_password` already uses (no `max_length` added to the schema itself — the fix is at the bcrypt call site, not input rejection). No authentication bypass at any point (never grants a session on the oversized path). |
| `E2E-W7-5-2T-RUNNER-READINESS-001` | Test infra | `run-w75-full-spec.sh` Postgres-readiness + init.sql-failure-hiding, closed | E2E runner reproducibility | Low | L1 | COVERED | `tests/e2e/scripts/run-w75-full-spec.sh` | 2026-08-03 | PASS: 4 consecutive back-to-back invocations, no manual pause, 10/10 passed / 0 skipped every time | CONFIRMED | — | `MONGLE-W7-5-BOARD-ROOM-RACE-AND-AUTH-HARDENING-001`, fixing `RE-QA-F-2T-RUNNER-FLAKY` (LOW) from `MONGLE-W7-5-FOCUSED-INDEPENDENT-RE-QA-001` (2 of 4 back-to-back runs previously failed: `pg_isready` reported ready before the server could actually run schema DDL, and `init.sql ... \|\| true` silently swallowed the resulting failure). Fixed: a real `SELECT 1` query-readiness gate beyond `pg_isready`, `-v ON_ERROR_STOP=1` with no error-swallowing, an explicit post-load `admin_auth` existence assertion, and a bounded 5-attempt retry around `alembic upgrade head` for the one remaining transient. Also fixed as part of the same finding: `tests/e2e/test-results/` added to root `.gitignore` and the previously-tracked `.last-run.json` untracked (`git rm --cached`, file kept on disk) so runs stop drifting a committed file — confirmed clean across all 4 runs of this verification. **Correction (`MONGLE-W7-5-HARDENING-QA-FAIL-REMEDIATION-001`, 2026-08-03, remediating `HARDENING-QA-F-002` FAIL from `MONGLE-W7-5-HARDENING-FOCUSED-INDEPENDENT-QA-001`):** the runner still redirected its backend/frontend logs to `/tmp/mc_w75_spec_runner_*.log`, outside the worktree, which blocked Independent QA from running the required 4 invocations at all under the repository-boundary policy. Fixed: logs now go to an absolute, timestamped, in-worktree, gitignored `tests/e2e/.runtime/w75-runner/run-<timestamp>-<pid>/` path (removed on a passing run, kept on a failing one). This fix's own first verification attempt used a relative path instead, which silently failed the log redirect inside the `cd backend`/`cd frontend` subshells and made all 10 Playwright tests fail on the very first re-run — found, root-caused, and fixed (absolute path + hardened fail-loud readiness checks) before re-verifying the full 4-consecutive-run result above. |
| `E2E-W7-5-3X3-VIEWPORT-001` | Frontend | Wagle board (`3c`/`3d`/`3e`) 3-viewport regression | Board/comment/reaction/Popular across viewports | Medium | L2 | COVERED | Ad hoc, uncommitted Playwright script (independently reimplemented per prior QA precedent, not a repository artifact) | 2026-08-03 | PASS: 3/3 viewports (390x844, 820x1180, 1180x820), each exercising post render / comment / reaction+Popular in sequence — 0 horizontal overflow, 0 console/page errors, Popular endpoint 200 with the created post present every time | CONFIRMED | — | `MONGLE-W7-5-BOARD-ROOM-RACE-AND-AUTH-HARDENING-001`'s own required regression sweep, against a dedicated disposable stack (Postgres 15496, backend 18097, Vite 5196), torn down after use. |
| `API-W7-5-MIGRATION-0021-PARTICIPANT-MERGE-MATRIX-001` | Backend | Migration 0021 participant-survivor merge algorithm, all 10 required data shapes | Board-room duplicate consolidation | High | L3 | COVERED | `backend/tests/test_migration_0021_participant_merge.py` | 2026-08-03 | PASS: 10/10 against real PostgreSQL (not mocks) — canonical/loser active/left in every combination, no-canonical-participant with one or multiple loser actives, the exact `HARDENING-QA-F-001` reproduction shape (canonical left + loser older-left-then-newer-active), multi-active read-state merge, multi-left lifecycle-timestamp merge, cross-family isolation | CONFIRMED | — | `MONGLE-W7-5-HARDENING-QA-FAIL-REMEDIATION-001`. Invokes the migration's own `_merge_duplicate_board_rooms` via `AsyncConnection.run_sync` (the same async/sync bridge Alembic's own `env.py` uses) against a `board_singleton_index_dropped` fixture that drops migration 0021's own unique index for the test's duration only, restored in a `finally` — confirmed not to bleed into other test files (49/49 passed when run alongside `test_wagle_integration.py`/`test_auth_admin_login.py`/`test_w75_phase_d_board_reactions.py`). One test (case 7) independently confirmed, via a throwaway copy of the pre-remediation algorithm run against this exact shape then rolled back, that the old code produced `{left: 2, active: 0}` — the same result `HARDENING-QA-F-001` reported — making this a genuine regression test, not a coincidentally-passing one. |

Evidence-backed Golden Journey candidates currently identifiable from the map:
`AUTH_LOGIN_JOURNEY` (`E2E-LOGIN-001`, `E2E-ADMIN-001`) and
`ADMIN_ENTRY_JOURNEY` (`E2E-ADMIN-001`). They are candidates only;
`GOLDEN_JOURNEY_FRAMEWORK: SKELETON_READY`.
