# MONGLE-W6-NONCANONICAL-UI-INCIDENT-001

- Status: INVALIDATED_ACTIVE_AUTHORITY
- Scope: Wave 6 UI only; no backend/API/DB change
- Canonical evidence: `engineering/phase2/evidence/mongle-wave6-tablet-canonical/`

The prior Wave 6 UI journey record claimed no PM-approved visual source was
available. That premise is false: the tracked canonical archive contains the
mobile structure source, tablet master, approved PNGs, token sources, manifest,
and provenance. Its connected screens are therefore invalidated as visual
authority, not as evidence that Account, Markpoint, Wagle, or access code exists.

Corrections: Account Login and optional FamilyGroup PIN are separate contracts;
A3 is canonical screen `1c` and A5 is `1e`; the mobile operational inventory is
66 canonical IDs plus states/steps, not a flat count; tokens are introduced
per-screen rather than wholesale. The active connected routes were detached and
the reusable API, session, authorization, and realtime code is parked for R8.

Required future order: R1 Account Login, R2 optional Family PIN, R3/R4
Markpoint User mobile/tablet, R5 Markpoint Admin, R6 Wagle GROUP, R7 Family
Home blocker review, R8 Target Backend Integration. Each visual task stops at
`READY_FOR_GPT_VISUAL_REVIEW`; PM alone decides its Visual Gate.
