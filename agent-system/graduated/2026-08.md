# Graduated — 2026-08

Only independently completed and archived tasks are listed here.

Graduation here means the task's own work is finished and its records are
synchronized. It does not by itself assert independent QA PASS for any claim
the task made — each row states its own evidence level.

| Task ID | Completed at | Final conclusion | Git ref | Handoff |
|---|---|---|---|---|
| `MONGLE-APPROVED-DECISIONS-FREEZE-AND-DECOMPOSITION-001` | 2026-08-01 | Docs/planning only. Phase A froze PM-approved D1–D8 into the Decision Freeze and reconciled the contract document set; Phase B recalculated the Epic/Feature decomposition, Wave plan, Implementation Backlog, Reset/Cutover Backlog and DoD/Test Matrix against them. Verdict `TARGET_DECISIONS_FROZEN` / `DECOMPOSITION_RECALCULATED`. Independently verified with corrections by `MONGLE-FREEZE-DECOMPOSITION-INDEPENDENT-VERIFICATION-001` (notably: the "0 remaining `PM_DECISION_REQUIRED`" claim undercounted 3 live non-D1–D8 items). PM accepted the decomposition with correction and released this task's `active.md` single-writer lock. No product code, commit, push, merge or PR. | `da7ea74` (unchanged — no commit made) | `engineering/phase2/MONGLE_APPROVED_DECISIONS_FREEZE_AND_DECOMPOSITION_REPORT.md` |
| `PHASE0-AGENT-SYSTEM-RECORD-INTEGRITY-AUDIT-001` | 2026-08-01 | Read-only Agent System registration-integrity audit. 10 findings (F1–F10): 9 documented tasks with zero `active.md`/`graduated/` registration, 2 stale isolated-worktree location claims, 1 missing Doran commit (`779cc70`), 1 undocumented external worktree, plus confirmed-clean areas. Its own core deliverable (writing the registrations) was correctly `BLOCKED` on another task's single-writer lock rather than overridden. Registration follow-through is inherited by `MONGLE-W1A-ACTIVE-REGISTER-CLOSEOUT-001` (F5/F6 applied) and a separate future task (F1–F4, the 9 registrations). Self-check only; never self-awarded a QA PASS. | `da7ea74` | `agent-system/handoffs/archive/2026-08/PHASE0-AGENT-SYSTEM-RECORD-INTEGRITY-AUDIT-001.md` |
| `MONGLE-FREEZE-DECOMPOSITION-INDEPENDENT-VERIFICATION-001` | 2026-08-01 | Independent read-only verification of the Decision Freeze and decomposition. Confirmed: D1–D8 all `APPROVED`/`FROZEN`, D6-P1–P8 register complete, D8 RESET and `MarkpointParticipant` supersede language consistent across all documents, Wave dependency structure sound with no inversion, cutover correctly sequenced after Wave 6 E2E, 5 Wave 1 READY tasks real. Corrected: the "0 `PM_DECISION_REQUIRED`" claim (≥3 live items remain, all non-blocking and outside D1–D8), and the 21/22/28 file-count discrepancy (28 confirmed correct by per-file blob-hash attribution, but conflates 6+ task-owned writers). Verdict `VERIFICATION_COMPLETE`. | `da7ea74` | `agent-system/qa/MONGLE-FREEZE-DECOMPOSITION-INDEPENDENT-VERIFICATION-001.md` (QA evidence serves as handoff) |
| `MONGLE-W1-START-GATE-AND-DB-CONTRACT-VERIFICATION-001` | 2026-08-01 | Independent read-only Wave 1 Start Gate and DB contract verification. Verdict `WAVE_1_START_GATE_CONDITIONAL` / `DB_CONTRACT_REQUIRES_CORRECTION` / `NOT_READY_FOR_FULL_WAVE_1`, PM-accepted as `CONDITIONAL_ACCEPTED_WITH_PM_CORRECTION`. Confirmed by direct source read that Account/FamilyGroup/FamilyMembership/Role/Permission Foundation is real and target-shaped, that no Account-native Credential or Session table exists, and that `resolve_current_account` currently resolves identity **only** through `LegacyIdentityMapping`. PM corrections recorded in the report's §25 addendum: operationally-ready task count is 0 (not 2), and `MONGLE-W1-FAMILY-SCOPED-ROUTE-AUTHZ-001` requires internal splitting since its own DoD demands Session revalidation. | `da7ea74` | `agent-system/qa/MONGLE-W1-START-GATE-AND-DB-CONTRACT-VERIFICATION-001.md` (QA evidence serves as handoff) |
| `MONGLE-W2-WAGLE-DURABLE-MESSAGING-AUTONOMOUS-001` | 2026-08-01 | Wave 2 Track A. Wagle durable messaging: message row, per-Room sequence advance and transactional Outbox append commit as one transaction, proven by a forced post-persist failure rolling back all three as seen from a second session; `client_message_id` idempotency (same body returns the same message with no second event, different body is 409); per-Room ordering with two rooms holding independent sequence spaces, which is what rules out an accidental global counter; forward-only read cursor; `/room-summaries` closing an N+1. PM rejected an initial `owner_service="doran"` and the emitted identifiers were corrected to `wagle` / `wagle.message.created` / `wagle_message`. Independent QA PASS by `MONGLE-PARALLEL-W2-W4-PARENT-INDEPENDENT-QA-001`. Outbox **consumption** is Wave 3 and was deliberately not covered. | `0d9280c` (unchanged — no commit made) | `agent-system/handoffs/archive/2026-08/MONGLE-W2-WAGLE-DURABLE-MESSAGING-AUTONOMOUS-001.md` |
| `MONGLE-W4-SERVICE-MARKPOINT-ACCESS-AUTONOMOUS-001` | 2026-08-01 | Wave 4 Track B. Markpoint access lifecycle on migration `0008`: member request → FamilyAdmin approve/reject, one PENDING request per family (idempotent for the same requester, 409 for another), reprocessing a decided request denied, per-member restriction/restore with history kept, cross-family denial in both directions. Constraints are DB-enforced (partial unique indexes, CHECKs, a composite FK for same-family enforcement), not application-only. Confirmed that access control creates **no** `MarkpointParticipant` aggregate and no Mission row, and that FamilyAdmin is never automatically ServiceAdmin. Independent QA PASS by the parent QA. Had no `active.md` record of its own — closed out here with its handoff archived. | `0d9280c` | `agent-system/handoffs/archive/2026-08/MONGLE-W4-SERVICE-MARKPOINT-ACCESS-AUTONOMOUS-001.md` |
| `MONGLE-PARALLEL-W2-W4-INTEGRATION-COMPLETION-001` | 2026-08-01 | Integration of the two lanes. Migration `0007` renamed the whole messaging runtime off its historical name in one atomic step — tables, indexes, constraints, trigger functions, permission codes, service code, route prefix and the Python module — with no alias, fallback, dual seed or translation map. Markpoint → Wagle relay implemented as port → composition-root adapter → application service, with a test asserting against the source that no Markpoint file imports a Wagle model. Two real defects were found by querying the PostgreSQL catalog rather than trusting migration logs: an Alembic revision id of 36 chars silently rolled back against `varchar(32)`, and trigger names derived from plural table names failed to match the singular real objects, briefly leaving both old and new guards live. Independent QA PASS. | `0d9280c` | `agent-system/handoffs/archive/2026-08/MONGLE-PARALLEL-W2-W4-INTEGRATION-COMPLETION-001.md` |
| `MONGLE-PARALLEL-W2-W4-PARENT-INDEPENDENT-QA-001` | 2026-08-01 | Independent QA of the parent bundle. Every product-contract check re-derived from a QA-created disposable database: migration `0007` verified by PostgreSQL catalog (zero historical-prefix rows across `pg_tables`/`pg_indexes`/`pg_constraint`/`pg_proc`/`pg_trigger`/`permissions`/`roles`), `0008` constraints, Track A durable messaging, Track B access lifecycle, and the relay boundary. Verdict `PARENT_INDEPENDENT_QA_CONDITIONAL`; PM accepted `FRONTEND_RUNTIME_FIX` and `TRACEABILITY_GAP`, and **retracted** this QA's own classification of ~127 frontend Doran occurrences as a non-blocking follow-up — that retraction is recorded in the report's §30 and produced the two frontend tasks below. | `0d9280c` | `agent-system/qa/MONGLE-PARALLEL-W2-W4-PARENT-INDEPENDENT-QA-001.md` (QA evidence serves as handoff) |
| `MONGLE-WAGLE-FRONTEND-IDENTIFIER-MIGRATION-001` | 2026-08-01 | Frontend Doran identifier retirement. All four PM target metrics **0** — active identifier, file/import, service-code literal, generated-API reference. `platform/doran/` → `platform/wagle/` (38 files via `git mv`, history preserved), `DoranLanding` → `WagleLanding`, every `Doran*` type renamed, and `generated/openapi.d.ts` regenerated from the live backend schema (it had described 41 `/doran/` routes the backend no longer serves). Fixed a HIGH runtime regression where the frontend compared the historical `service_code` and reported every active Wagle subscription as unavailable — a defect no type check can catch, since both sides are `string`; three new backend contract tests now pin the seam. A bulk sweep broke the legacy redirect mid-task and was caught and repaired. Independent QA PASS via `MONGLE-COMBINED-NAMING-AND-FRONTEND-INDEPENDENT-QA-001` + `MONGLE-NAMING-CLOSEOUT-E2E-ENVIRONMENT-RECOVERY-001`. | `0d9280c` | `agent-system/handoffs/archive/2026-08/MONGLE-WAGLE-FRONTEND-IDENTIFIER-MIGRATION-001.md` |
| `MONGLE-NARAN-RUNTIME-RETIREMENT-AND-FRONTEND-CLOSEOUT-001` | 2026-08-01 | Naran platform-name retirement, closing the naming campaign on both axes. PM released the two remaining allowlist keeps: `/naran/*` routes removed **outright** — no redirect, alias or fallback, old bookmarks 404 by explicit cutover — and the persisted `naran.activeFamily.*` key migrated **one-time** to `mongle.activeFamily.*` then deleted. The value is carried forward *before* the old key is removed, because simply dropping it would silently reset every existing user's selected Family. Naran Zero Gate all 0 across 12 categories; three non-active categories (storage-migration source, prohibition strings, historical archive) counted separately and explicitly **not** claimed as zero. Also resolved the predecessor's `PRODUCTION_BUILD` blocker by building in a throwaway workspace without touching the host's `node_modules`. Its rewritten E2E specs were left unexecuted and were executed by the task below. | `0d9280c` | `agent-system/handoffs/archive/2026-08/MONGLE-NARAN-RUNTIME-RETIREMENT-AND-FRONTEND-CLOSEOUT-001.md` |
| `MONGLE-COMBINED-NAMING-AND-FRONTEND-INDEPENDENT-QA-001` | 2026-08-01 | Combined independent QA of the two frontend naming tasks. Passed every static and source-level contract — Wagle Zero Gate, Naran Runtime Zero Gate, route-retirement and storage-migration source contracts — and correctly **refused to substitute static evidence for required E2E**, reporting `BLOCKED` rather than a conditional pass. Found that `start-mongle-phase1.sh` reused a stale frontend image and invalidated its own 41-failure Playwright result rather than reporting it as product failure. Left three verifications unexecuted (current-source Playwright, clean isolated build, focused backend seam), which is precisely what the task below executed. | `0d9280c` | `agent-system/qa/MONGLE-COMBINED-NAMING-AND-FRONTEND-INDEPENDENT-QA-001.md` (QA evidence serves as handoff) |
| `MONGLE-NAMING-CLOSEOUT-E2E-ENVIRONMENT-RECOVERY-001` | 2026-08-01 | E2E environment recovery and final closeout of the naming bundle. Executed all three previously-blocked verifications: clean isolated production build (context 106.98 MB → 28.18 kB, `npm ci` from lockfile, container-only `node_modules`, host tree never touched), current-source Playwright (**90 collected / 86 passed / 0 failed / 4 skipped / 0 retries**), and the focused backend Wagle seam test (**3/3**). Corrected the predecessor's diagnosis on two points: the "BuildKit stall" was a 54.4 s registry metadata fetch, not the build context; and `--build` closed only one stale-image path — `reuseExistingServer: true` was the wider one, since Playwright skips the start script entirely when the port already answers. Introduced a source-fingerprint guard proving worktree ⇄ image ⇄ browser agreement, **verified by making it fail** against a deliberately planted stale image. Also found that `COPY . .` was layering the host's darwin-arm64 `node_modules` over the container's `npm ci` tree, and that the repository declared no test dependencies at all despite two documents specifying `python3 -m pytest -q`. One stale test expectation was found to be pinning a privilege escalation in place (a FAMILY owner asserted to see "미션 관리", which migration `0006` correctly removed) and was **inverted, not deleted**. Verdict `E2E_ENVIRONMENT_RECOVERY_PASS` / `MONGLE_PARALLEL_W2_W4_PARENT_COMPLETE`. No product source changed. | `0d9280c` | `agent-system/handoffs/archive/2026-08/MONGLE-NAMING-CLOSEOUT-E2E-ENVIRONMENT-RECOVERY-001.md` |
| `MONGLE-PARALLEL-W2-W4-001` | 2026-08-01 | **Parent parallel-execution bundle — COMPLETE.** A coordination identifier for Track A (Wagle durable messaging), Track B (Markpoint access), their integration, and the frontend naming work that the parent QA's retraction pulled into scope. All member tasks graduated above. Stated plainly so the basis is not overread: the final recovery task did **not** re-verify migrations `0007`/`0008` or the two lanes — that was explicitly out of its scope. The bundle is complete because the three verifications the parent QA left unexecuted are now executed, not because everything was re-run. Outstanding and carried forward: `TRACEABILITY_GAP` — the entire bundle is still uncommitted working state at `0d9280c`; PM performs commit/push. | `0d9280c` (unchanged — no commit made) | `agent-system/handoffs/archive/2026-08/MONGLE-PARALLEL-W2-W4-001.md` |
| `MONGLE-W1A-ACTIVE-REGISTER-CLOSEOUT-001` | 2026-08-01 | Governance closeout (Wave 1A). Graduated the Freeze task and released its `active.md` single-writer lock; recorded the three verification tasks above as completed QA evidence; applied the two factual location corrections (A1 and DATA-A isolated-worktree claims, both paths confirmed absent and both file sets confirmed merged into this worktree) and added the missing `779cc70` to the Doran R2 record. Deliberately out of scope by PM decision: the 9 unregistered tasks from audit findings F1–F4, deferred to their own task. No product code, test, DB, migration or commit. | `da7ea74` (unchanged — no commit made) | `agent-system/handoffs/archive/2026-08/MONGLE-W1A-ACTIVE-REGISTER-CLOSEOUT-001.md` |
